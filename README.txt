To run a file:

Must have Python 2.7 installed
Run: python <filename>.py
If the app server is successfully stared, it will display:
http://0.0.0.0:8080

NOTE: The app will not actually be available at http://0.0.0.0:8080
You have to use http://localhost:8080